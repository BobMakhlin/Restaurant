<EditForm Model="@ingredient" OnValidSubmit="@OnValidSubmit">
    <DataAnnotationsValidator />
    <div class="form-group">
        <label>Title :</label>
        <div>
            <InputText class="form-control" @bind-Value="@ingredient.Title" />
            <ValidationMessage For="@(() => ingredient.Title)" />
        </div>
    </div>
    <div class="form-group">
        <label>Photo :</label>
        <div>
            <InputText class="form-control" @bind-Value="@ingredient.Photo" />
            <ValidationMessage For="@(() => ingredient.Photo)" />
        </div>
    </div>
    <div class="form-group">
        <label>Price :</label>
        <div>
            <InputNumber class="form-control" @bind-Value="@ingredient.Price" />
            <ValidationMessage For="@(() => ingredient.Price)" />
        </div>
    </div>
    <button type="submit" class="btn btn-success">
        @ButtonText
    </button>
</EditForm>
@code {
    [Parameter] public IngredientPoco ingredient { get; set; }
    [Parameter] public string ButtonText { get; set; } = "Save";
    [Parameter] public EventCallback OnValidSubmit { get; set; }
}